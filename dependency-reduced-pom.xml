<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.example</groupId>
  <artifactId>sample-dropwizard-service</artifactId>
  <version>1.0-SNAPSHOT</version>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven.shade.plugin.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer />
                <transformer>
                  <mainClass>com.example.helloworld.HelloWorldApplication</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <createDependencyReducedPom>true</createDependencyReducedPom>
          <filters>
            <filter>
              <artifact>*:*</artifact>
              <excludes>
                <exclude>META-INF/*.SF</exclude>
                <exclude>META-INF/*.DSA</exclude>
                <exclude>META-INF/*.RSA</exclude>
              </excludes>
            </filter>
          </filters>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-jar-plugin</artifactId>
        <version>${maven.jar.plugin.version}</version>
        <configuration>
          <archive>
            <manifest>
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
            </manifest>
          </archive>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <repositories>
    <repository>
      <id>sellular-commons-github</id>
      <url>https://maven.pkg.github.com/vishal-sangam-lg/sellular-commons</url>
    </repository>
    <repository>
      <id>sellular-schemas-github</id>
      <url>https://maven.pkg.github.com/vishal-sangam-lg/sellular-schemas</url>
    </repository>
    <repository>
      <id>confluent</id>
      <url>https://packages.confluent.io/maven/</url>
    </repository>
  </repositories>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.dropwizard</groupId>
        <artifactId>dropwizard-bom</artifactId>
        <version>${dropwizard.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <properties>
    <guice.version>7.0.0</guice.version>
    <commons-collections.version>4.4</commons-collections.version>
    <hikaricp.version>6.3.0</hikaricp.version>
    <sellular-schemas.verions>1.0.4</sellular-schemas.verions>
    <dropwizard.version>4.0.13</dropwizard.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.jar.plugin.version>3.2.0</maven.jar.plugin.version>
    <modelmapper.version>3.2.2</modelmapper.version>
    <maven.shade.plugin.version>3.2.4</maven.shade.plugin.version>
    <kafka-protobuf-serializer.version>7.5.0</kafka-protobuf-serializer.version>
    <sellular-commons.version>1.0.12</sellular-commons.version>
    <lombok.version>1.18.30</lombok.version>
    <maven.compiler.target>21</maven.compiler.target>
    <postgresql.version>42.7.5</postgresql.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>
</project>
